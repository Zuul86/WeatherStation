# Mosquitto password file
# Generate entries with: docker run --rm eclipse-mosquitto mosquitto_passwd -b /dev/stdout <username> <password>
# Example: mosquitto_passwd -b passwd telemetry secretpassword
# Then restart the mqtt-broker service.
