#ifndef SECRETS_H
#define SECRETS_H

// Wi-Fi Configuration (Fallback if WiFiManager is not used)
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Sensor Settings
#define DHT_PIN 2            // Pin connected to DHT22 data line
#define SDA_PIN 4            // I2C SDA Pin (for BMP180)
#define SCL_PIN 5            // I2C SCL Pin (for BMP180)
#define ALTITUDE_METERS 100.0 // Your local altitude in meters for pressure calculation
#define LAT 37.7749          // Station Latitude
#define LONG -122.4194       // Station Longitude
#define DATA_COLLECTION_INTERVAL 60000 // Ingestion interval (milliseconds)

// MQTT Configuration
#define MQTT_SERVER "YOUR_MQTT_SERVER_IP_OR_HOST"
#define MQTT_PORT 1883       // 1883 for non-TLS, 8883 for TLS
#define MQTT_CLIENT_ID "WeatherStationESP8266"
#define MQTT_USER "YOUR_MQTT_USER"
#define MQTT_PASSWORD "YOUR_MQTT_PASSWORD_OR_SAS_TOKEN"

// SSL/TLS Configuration (optional but recommended for production)
// The SSL certificate fingerprint of the MQTT broker (e.g. Azure Event Grid or Mosquitto TLS)
#define MQTT_SSL_FINGERPRINT "XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX XX"

#endif
