import { ConvertTemperaturePipe } from './convert-temperature.pipe';

describe('ConvertTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
